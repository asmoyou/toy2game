# Toy2Game Agent 开发规范

本文件适用于整个仓库，供后续新增游戏、修改现有游戏和维护大厅时参考。具体需求以用户当前说明为准；接入步骤见 [docs/adding-games.md](docs/adding-games.md)，游戏规则见各游戏的 `README.md`。发现文档与代码不一致时，先核对实现及测试，再同步本次修改涉及的说明。

## 1. 项目定位与复现原则

- 将实体玩具转化为打开即可游玩的中文网页游戏，面向儿童、家庭和朋友同屏娱乐。首屏进入实际游戏，主体玩具必须清晰可见、可以操作。
- 优先适配 iPad Safari 的横屏触控，同时支持平板竖屏、手机横竖屏和桌面鼠标操作。
- 当前网站为纯静态客户端，无账号、后端或异地联机服务。现有两款游戏支持 2 至 4 个同屏席位及电脑对手；新游戏的人数和模式按其规则确定。
- 先还原玩具的可辨识造型、关键部件和核心互动，再增加数字版特有的玩法。新增机制需有明确规则、反馈和验证，避免装饰或随机事件盖过原玩法。
- 从参考图中区分“可以直接观察的外观”和“尚未确定的规则”。图片无法确定的配件用途、行动顺序、随机分布和胜负条件，不能当成已知事实。
- 开发前在游戏 `README.md` 记录参考来源、规则依据、数字版改编和关键假设。会改变核心玩法的缺失信息应尽早澄清，独立的建模和工程准备可继续推进。
- 例如 [平衡太空人参考图](待转换玩具/平衡太空人.png) 可以用于提取青色平台、中央支撑、不同面罩颜色的太空人及配件造型；放置或取走的顺序、骰子和转盘的具体用途、倾倒后的结算方式仍需规则依据。

## 2. 开工前阅读

1. 查看 `git status --short`，识别用户已有改动，并阅读任务目录内更具体的规范。
2. 阅读根目录 [README.md](README.md)、[新游戏接入约定](docs/adding-games.md) 和相关游戏的 `README.md`、`package.json`。
3. 阅读实际入口、规则或物理模块、场景、样式及相关测试，了解状态如何流转后再修改。
4. 接入游戏时核对 `packages/catalog/index.ts`、`packages/catalog/games.json` 和 `scripts/catalog.mjs`，不能只复制文档示例。

保留已有未提交改动。按任务范围修改，避免顺带重排全仓库格式、统一旧引擎版本或重构无关游戏。

## 3. 目录与技术边界

| 位置 | 职责与参考方式 |
| --- | --- |
| `apps/web/` | React + Vite 中文大厅；维护搜索、分类、排序、收藏、最近游玩和随机入口 |
| `games/penguin-ice/` | TypeScript + Three.js + cannon-es；参考刚体模拟、稳定后换人、电脑调度和物理回归 |
| `games/rabbit-trap/` | React + Three.js + boardgame.io + GSAP；参考回合规则、动画结算、存档、机关和触控交互 |
| `packages/catalog/` | 游戏登记表、类型、站点文案和浏览器端收藏及访问记录；不放游戏内部规则 |
| `scripts/` | 登记校验、统一开发、构建、预渲染、SEO、预览、游戏测试调度和封面截图 |
| `tests/` | 登记契约和整个生产站点的浏览器测试 |
| `待转换玩具/` | 待制作玩具的参考资料，不是生产静态资源目录 |

- 使用 npm workspaces，范围为 `apps/*`、`games/*`、`packages/*`。Node.js 最低版本为 `22.18.0`，优先遵循 `.node-version` 的 `22.21.1`。
- 全仓库共用根目录 `package-lock.json`。依赖声明放在使用它的 workspace；新增或调整依赖时在根目录执行 `npm install` 更新锁文件，不新增游戏专属锁文件。
- 大厅和游戏是独立 HTML 入口，通过普通链接跳转。大厅不能导入游戏源码、Three.js 或物理引擎，也不使用 iframe 承载游戏。
- 新游戏使用 Vite + TypeScript，开启严格类型检查；是否使用 React 按界面复杂度和相近游戏决定。
- 3D 场景使用 Three.js。刚体玩法优先参考现有 cannon-es，规则棋盘优先参考现有 boardgame.io；使用成熟引擎，游戏专属规则和参数留在本游戏内。
- 各游戏保留自己的依赖版本、样式、生命周期和测试。不直接跨游戏导入内部模块；只有实际共用的站点能力才抽到 `packages/`。
- 延续所修改文件的命名、引号和格式习惯。复杂的物理近似、坐标转换或时序处理可加简短注释，说明原因和边界。

## 4. 规则、物理与状态

- 按职责组织规则或物理、Three.js 场景、UI 调度、音效、测试。可参考 `game.ts`、`physics.ts`、`scene.ts`、`main.ts` 或 `App.tsx`，无需为了统一文件名强行拆分。
- 规则状态是合法操作和胜负的依据，场景呈现状态与过程。避免从 DOM 文案、动画进度或临时网格位置反推回合规则。
- 明确等待输入、电脑思考、动画或物理结算、暂停、结束等阶段。结算期间锁住重复操作；连续碰撞、掉落或机关完成后再切换玩家。
- 物理玩法由质量、重心、约束、碰撞、摩擦等实际状态决定结果。视觉晃动可以增强反馈，但不能用预设手数或无关随机数替代失衡、滑落或坍塌判定。
- 使用固定物理步长并限制补算次数，防止低帧率或恢复页面时一次推进过久。稳定判定应考虑相关物体和连锁事件，失败归属保持为触发该事件的玩家。
- 真人与电脑使用相同的合法动作入口。电脑回合禁用真人代操作；电脑决策不得阻塞渲染。
- 暂停、打开阻断操作的弹窗或页面进入后台时，暂停会推进对局的物理、天气和电脑计时。关闭弹窗后还需检查页面是否可见。
- 重开、切换阵容和销毁场景时，取消旧计时器、异步回调和动画，防止上一局的动作影响新局。
- 随机规则应可用固定种子或可注入随机源重现。存档恢复需要保持规则相关的随机状态，不能因刷新重新抽取已确定的结果。

## 5. 画面与交互

- 以可辨认的真实游戏场景为视觉主体，保持玩具比例、部件关系和可操作位置清楚。可用程序化 Three.js 模型或本地素材；不要用参考图片替代可玩的场景。
- 游戏各自保留主题色和角色表现，共同保持中文界面、清晰的当前玩家、主要动作、剩余状态和结束结果。颜色之外同时使用名字、编号或图标区分玩家。
- 主场景使用开阔布局，不塞进装饰卡片。工具栏、玩家栏、状态提示和弹窗要给棋盘留出空间，不能遮住关键操作位置。
- 工具按钮优先使用现有 `lucide` 或 `lucide-react` 图标，提供中文 `aria-label` 和 tooltip。模式用分段控件或选择器，开关用 checkbox 或 switch，数值用合适的输入控件。
- 全屏按钮按浏览器能力显示：确认页面全屏接口可用且权限允许，不支持或被禁用时直接隐藏，不保留无效入口。兼容旧版 Safari 时，同时核对其能力标志及成对的进入、退出接口。全屏状态以浏览器事件为准，外部退出时同步图标、中文名称和 tooltip；请求失败给出轻量提示，不改变对局或相机。
- 同屏游戏的人数和席位类型沿用企鹅式交互：人数较少时用分段选择，各可配置席位并列展示带图标的“真人／机器人”，明确选中状态及所属玩家，不使用含义隐晦的电脑勾选框。展示真人和机器人数量；人数范围、首位是否固定真人及是否支持全电脑对战以各游戏实际规则为准，不为统一界面删减已有模式。
- 人数、席位及其他开局选项先编辑草稿，关闭或取消不影响当前对局，确认开新局后才应用并保存。新增席位默认真人；同一次设置中减少人数再增加，应恢复暂时隐藏席位的选择，提交时仅启用所选人数。弹窗期间暂停电脑操作，确认重开时取消旧局回调；企鹅首位固定真人，小兔子保留所有席位可配置及全电脑对战。
- “游戏设置”和“新的一局”使用独立入口。设置用于编辑人数、阵容、棋盘或天气等开局选项，提交按钮明确表示将按草稿开始新局；“新的一局”及结算后的“再来一局”沿用当前已生效配置，不先展示配置表。有进度且未结束的对局重开前须确认，取消后保留进度；尚未产生进度或已结束时可直接重开。结算中可另设“换个阵容”入口。音效、全屏等即时偏好不触发对局重置。
- 使用原生按钮和链接，保留清楚的焦点状态、禁用状态和选中状态。弹窗提供可访问标题、关闭操作和关闭后的焦点恢复。
- 主要触控目标以至少 44 CSS px 为目标。棋子较小时扩展拾取范围或提供等效选择控件，不依赖 hover 才能完成操作。
- 区分点击、拖动、多指缩放及 `pointercancel`；相机操作结束时不能误触游戏动作。3D 视角应支持按玩法需要旋转、缩放和恢复默认视角。
- 使用稳定尺寸、`minmax(0, 1fr)`、明确的画布容器和响应式断点。兼顾 `100dvh`、安全区和屏幕旋转，避免文字溢出、工具挤压、横向滚动及动态内容导致布局跳动。
- 音效在用户手势后解锁，提供静音及偏好保存；遵循减少动态效果偏好。加载失败或 WebGL 不可用时显示可理解的错误状态和恢复入口。
- 场景重置和销毁时释放几何体、材质、纹理、控件、监听器及动画帧；限制设备像素比和昂贵效果，特别关注平板性能。

## 6. 新游戏接入契约

1. 新建 `games/<id>/`，ID 使用小写英文、数字和连字符，以字母开头，发布后保持稳定。目录名、登记 ID、公开路径应一致。
2. 提供 `package.json`、`index.html`、`src/`、`public/`、TypeScript 配置、`README.md` 和适合该游戏的测试。
3. 包名为 `@toy2game/<id>`，设为私有 ESM 包，声明 `@toy2game/catalog` 依赖。必须提供 `dev` 和 `build`；构建需先通过类型检查。
4. `dev` 接受 `--base`、`--port`、`--strictPort`；`build` 接受 `--base`，输出到该游戏自身的 `dist/`。统一脚本会传入这些参数。
5. 在唯一登记表 `packages/catalog/games.json` 加入条目，放入实际游戏封面，并接入下节中的大厅链接及游玩记录。
6. 更新涉及的测试、封面选择器和文档，完成单独运行及站点嵌套路径验证后再交付。

登记表必填字段以 `GameDefinition` 和 `validateRegistry()` 为准，目前包括：

| 字段 | 要求 |
| --- | --- |
| `id` | 与游戏目录一致，唯一且符合 slug 格式 |
| `title`、`englishTitle`、`description` | 真实的游戏名称与简介 |
| `category` | 当前为 `party` 或 `strategy`；`all` 只是大厅筛选项 |
| `tags`、`players`、`duration` | 已实现玩法的标签、人数和时长，不提前宣称尚未支持的能力 |
| `cover` | `images/<name>.png`、`.webp` 或 `.jpg`，对应大厅 `public/` 下真实文件 |
| `color` | 当前为 `ice` 或 `garden`，对应大厅封面样式 |
| `addedAt` | 有效的 `YYYY-MM-DD` 日期 |
| `seo` | 必须包含非空的 `title`、`description`、`recommendation`，以及非空字符串数组 `rules`、`controls` |

新增分类或封面配色时，同时更新 `packages/catalog/index.ts`、`scripts/catalog.mjs` 及大厅相关样式和测试。站点级文案集中在 `packages/catalog/site.json`，页面元数据应与登记信息和实际玩法一致。

- 新游戏的 `vite.config.ts` 从 `../../scripts/seo.mjs` 引入 `seoPlugin`，注册 `seoPlugin({ gameId: '<id>' })`，框架插件照常保留。元数据与文字版由共享逻辑生成，不另维护一套游戏简介。
- 游戏 `index.html` 的应用根节点内保留 `<!-- game-summary -->`，供插件注入可直接读取的游戏摘要；客户端启动时挂载实际游戏。不要重复硬编码由插件生成的 title、description 或结构化数据。
- 大厅构建通过 `scripts/prerender.mjs` 和 `apps/web/src/entry-server.tsx` 预渲染。保留 HTML 根节点占位符；大厅首轮渲染不能直接访问 `window`、`document` 或本地存储，浏览器状态在客户端初始化。
- 正式站点地址通过 `SITE_ORIGIN` 或 `packages/catalog/site.json` 的 `origin` 配置，路径前缀使用 `SITE_BASE`。没有公开域名时不编造地址；未设置 origin 时当前构建省略 canonical、绝对社交图片地址和 sitemap。

## 7. 路由、资源与本地存储

在游戏入口接入以下共享 API，将示例 ID 替换为实际 ID：

```ts
import { libraryUrl, recordVisit } from '@toy2game/catalog/browser';

recordVisit('your-game-id');
const home = libraryUrl(import.meta.env.BASE_URL);
```

- 标题链接或返回按钮使用 `home`，可访问名称为“返回游戏大厅”。不要额外添加遮挡场景的公共悬浮栏。
- 大厅通过 `gameUrl(id, base)` 生成入口，游戏地址为 `<base>games/<id>/`，保留末尾 `/`。支持 `SITE_BASE` 前缀，不能只在站点根路径验证。
- HTML 公共资源使用 `%BASE_URL%`，JS 中的 `public/` 资源使用 `import.meta.env.BASE_URL`；源码素材优先 `import` 或 `new URL(..., import.meta.url)`。运行时不能硬编码 `/assets/` 或 `/fonts/`。
- 封面使用实际游戏截图，建议 1200 × 800，主体居中以适应裁切。产品参考图仅作为设计输入，不直接作为发布封面；新增字体、模型或图片保留来源及许可证。
- 收藏和最近游玩使用 `@toy2game/catalog/browser`，不复制一套站点存储逻辑。该模块依赖浏览器环境，不在构建期渲染时调用存储或导航 API。
- 新游戏存储键采用 `<id>-<purpose>-v1` 等命名，隔离设置与对局。已有 `penguin-preferences`、`little-rabbit-match-v1` 等键保持兼容，不为了统一名称直接替换。
- 读取存储时捕获异常、校验版本和结构；损坏或不可用的存储不能阻止开局。修改存档结构时提供迁移或明确的兼容处理。
- 是否保存完整对局按游戏需求决定：企鹅目前保存偏好，小兔保存完整规则状态。不要把刚体瞬时位置当成足够恢复物理世界的完整存档。
- 根构建产物为 `dist/`，部署方式见 [docs/cloudflare.md](docs/cloudflare.md)。保持多入口静态路由与真实 404；新增游戏通常不需要修改 Worker 配置。

## 8. 运行与验证

以下命令均从仓库根目录执行。已有锁文件且只需安装依赖时使用 `npm ci`；本次改变依赖声明时使用 `npm install`。

```sh
npm run dev
npm test
npm run build
npm run test:e2e
```

- `npm run dev` 自动发现登记游戏并提供统一入口，默认 `http://localhost:5173/`，占用时选择空闲端口，以实际输出为准。单独开发可用 `npm run dev --workspace games/<id>`。
- `npm test` 运行根目录登记契约及各 workspace 提供的 `test`，不包含全部浏览器测试。局部逻辑修改可先运行 `npm test --workspace games/<id>`。
- `npm run test:e2e` 默认启动生产预览，必须先完成 `npm run build`，不能拿旧 `dist/` 验证新改动。默认用本机 Chrome；无 Chrome 时安装 Chromium，并用 `PLAYWRIGHT_CHANNEL=chromium npm run test:e2e`。
- 根浏览器测试当前覆盖 1440 × 1000 桌面和 390 × 844 手机；触控游戏还需验证 iPad 横屏 1180 × 820、竖屏 820 × 1180，必要时覆盖 1024 × 768 和手机横屏。
- 游戏详细浏览器测试通常依赖开发模式诊断接口。先启动开发服务，再用实际大厅地址执行 `SITE_URL=http://localhost:5173/ npm run test:games`。它按游戏选择 `test:browser` 或 `test:e2e`，并传入 `GAME_URL`。
- `test:games` 不会执行企鹅的 `test:bots`、`test:experience` 或 `test:balance`。修改对应行为时，按该游戏 README 补跑相应脚本；需要 WebKit 时先执行 `npx playwright install webkit`。
- 浏览器测试接受 `GAME_URL`，页面导航使用相对路径 `page.goto('./')`，同时适配独立游戏与统一站点。`window.__iceGame`、`window.__rabbit` 等诊断接口仅在 `import.meta.env.DEV` 下暴露，生产测试使用真实界面操作。

新增游戏时必须检查这些尚未完全自动化的位置：

| 文件 | 接入时检查 |
| --- | --- |
| `tests/browser/games.spec.ts` | 增加新游戏及其画布、主要操作、弹窗和返回大厅测试；不能让新游戏误用现有二选一分支 |
| `tests/browser/library.spec.ts` | 游戏数量、分类数量和随机入口正则目前含两个游戏的假设，需按登记表更新 |
| `scripts/capture-covers.mjs` | 当前按企鹅或小兔选择场景容器，需增加新游戏的实际选择器；启动开发服务后用 `npm run covers` 截图 |

按风险验证，要求如下：

- 规则或物理测试覆盖合法与非法操作、回合转换、胜负边界、随机可复现性及本次变更的关键场景。平衡类玩法应覆盖稳定、偏载、失衡、连锁掉落与结算归属等适用场景。
- 浏览器验证覆盖加载完成、无页面异常或本地资源请求失败、主要操作、暂停恢复、重开、已实现的电脑行为与持久化、大厅往返和最近游玩。
- 修改全屏或阵容设置时，覆盖不支持及权限禁用时隐藏、支持时进入与退出、失败反馈；人数增减后的席位保留、取消不改局、确认重开与刷新恢复，以及设置弹窗和后台期间电脑不会推进旧局。兼容旧版 Safari 接口时增加对应验证，并检查手机与 iPad 横竖屏的设置弹窗、选中状态和触控尺寸。
- 修改设置或重开入口时，分别验证配置草稿的应用、原配置重开、重开确认的取消、未开局及结算后的直接重开。确认框期间暂停电脑和天气，确认后取消旧局任务；取消的配置草稿不得混入之后的重开。增加工具按钮后检查手机工具栏的触控尺寸及溢出。
- 3D 验证必须包含画布像素检查、操作前后变化，以及桌面和移动端截图的实际查看。只检查 `canvas` 存在或截图文件生成不足以证明场景正确；检查主体完整、素材可见、文字无溢出且控件不遮挡操作。
- 改路由、构建或资源路径时，还需验证非根 `SITE_BASE`、游戏直接访问和刷新、未知路径返回 HTTP 404。
- 新游戏和共享行为改动完成后运行根测试、生产构建及相关浏览器回归。纯文档修改只需核对命令、路径和内容一致性，无需为此新增测试或启动服务。
- 截图、报告和构建输出留在被忽略的 `artifacts/`、`test-results/`、`playwright-report/`、`dist/` 等目录，不作为源码提交。大厅正式封面除外。

## 9. 交付要求

- 新游戏需有可完整进行到结算并再次开局的玩法、可操作的模型、必要的设置和真实反馈；提供的每个控制项都应生效。
- 游戏 `README.md` 更新实际规则、与参考玩具的差异、运行及验证命令、资源来源和已知限制。站点接入约定变更时同步相关文档。
- 完成可运行游戏改动后启动本地开发服务，提供实际 Local 地址，需要跨设备测试时同时给出 Network 地址；不能覆盖用户已经占用的端口或停掉无关服务。
- 交付说明写清改动、已完成验证及未验证项。浏览器设备模拟不能声称为真实 iPad 硬件测试；构建通过也不能替代实际游玩验证。
